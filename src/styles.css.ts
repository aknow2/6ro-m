import { globalStyle } from '@vanilla-extract/css';

globalStyle('body', {
  backgroundColor: 'black',
  margin: 0,
  padding: 0,
});

globalStyle('canvas', {
  backgroundColor: 'white',
  borderRadius: '50%',
});
